Quickstash.com/
├── frontend/
│   ├── package.json
│   └── src/
├── backend/
└── netlify.toml 
[build]
  base = "frontend"
  publish = "build"
  command = "npm install && npm run build"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
