{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build",
    "startCommand": "node backend/server.js"
  },
  "env": {
    "NODE_ENV": "production",
    "PORT": "8080"
  }
}
